export const contains = (text, pattern) => text.indexOf(pattern) >= 0;
export function lol(bar) {
    if (false || Math.random() > 0.5) {
        return { bar: 3 };
    }
    return { bar: 4 };
}
export function cool(bar) { return Number(bar); }
